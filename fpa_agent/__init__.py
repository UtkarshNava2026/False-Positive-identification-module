"""
False Positive Identification Agent.

Heavy modules (cv2, torch) are imported from submodules on demand — not here —
so detection.py can configure Qt before OpenCV loads.
"""

__version__ = "1.0.0"
